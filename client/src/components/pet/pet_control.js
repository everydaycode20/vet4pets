import React from "react";

const PetControl = ({  }) => {

    return (
        <div className="pet-control">
            <span>125 pets</span>
            <button>Add new pet</button>
        </div>
    );
};

export default PetControl;